global:
  scrape_interval: 10s
  evaluation_interval: 10s

scrape_configs:
  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']
  # Сюда в будущем добавим endpoints Go микросервисов,
  # или Otel Collector, который будет отдавать метрики.