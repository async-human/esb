apiVersion: 1

providers:
  - name: 'Default Dashboards'
    orgId: 1
    folder: ''
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10 # Grafana будет раз в 10 сек проверять файлы на изменения
    options:
      path: /etc/grafana/provisioning/dashboards