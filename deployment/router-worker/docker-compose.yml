services:
  router-worker:
    build:
      context: ../..
      dockerfile: deployment/router-worker/Dockerfile
    container_name: ${COMPOSE_PROJECT_NAME}-router-worker
    env_file:
      - .env
    working_dir: /app
    networks:
      - esb_network
    depends_on:
      otel-collector:
        condition: service_started
